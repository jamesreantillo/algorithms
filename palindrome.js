const str = 'bajames';

// Answer1
// const palindromeStr = (x) => {
//   let revStr = x.split('').reverse().join('');
//   return revStr === x;
// };

// console.log(palindromeStr(str));

// Answer2
// const palindromeStr = (x) => {
//   let revStr = x.split('');
//   return revStr.every((z, i) => {
//     return z === revStr[revStr.length - i - 1];
//   });
// };

// console.log(palindromeStr(str));
